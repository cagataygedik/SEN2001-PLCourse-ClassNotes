from random import randint
from math import sqrt, cos
# we don't have to import everything from a class, we can just import one thing.

a = randint(100, 120)
print(a)
print(sqrt(5.55))
print(cos(90))
